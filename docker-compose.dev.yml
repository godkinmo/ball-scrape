version: '2'
services:
  express:
    extends:
      file: docker-compose.base.yml
      service: express
  nuxt:
    extends:
      file: docker-compose.base.yml
      service: nuxt
    ports:
     - "${APP_PORT}:80"
     - "${APP_SECURE_PORT}:443"
networks:
  sdnet:
    driver: "bridge"
