version: '2'
services:
  app:
    build:
      context: ./docker/app
      dockerfile: Dockerfile
    image: ball-scrape/app
    restart: always
    environment:
      APP_ENV: "${APP_ENV}"
      XDEBUG_HOST: "${XDEBUG_HOST}"
    volumes:
     - ./express:/var/www/html/express:cached
     - ./nuxt:/var/www/html/nuxt:cached
    networks:
     - sdnet
  express:
    build:
      context: ./docker/express
      dockerfile: Dockerfile
    image: ball-scrape/express
    restart: always
    volumes:
     - ./express:/var/www/html/express:cached
    networks:
     - sdnet
  nuxt:
    build:
      context: ./docker/nuxt
      dockerfile: Dockerfile
    image: ball-scrape/nuxt
    restart: always
    volumes:
     - ./nuxt:/var/www/html/nuxt:cached
    networks:
     - sdnet
